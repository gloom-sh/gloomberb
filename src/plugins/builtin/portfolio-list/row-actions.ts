/**
 * The pane's own keys for its row menu actions, shown beside them in the pane
 * menu. The table and the grid both open the cursor ticker in a new floating
 * pane on Shift+Enter.
 */
export const PORTFOLIO_ROW_MENU_KEYS: Readonly<Record<string, string>> = {
  "ticker:pin-floating": "Shift+Enter",
};
